# List all resources and their informations like short names
# You can also find out which API group a resource is belong to, for example, pods are in default('') group and deployments are in `apps` group
# apiVersion value if prefixed with API group name.
kubectl api-resources

# List all api versions the cluster supports
kubectl api-versions

